#include "main.h"
/**
  * positive_or_negative - Checks for positive or negative number
  * @i: Always number
  * Return: 0
  */
void positive_or_negative(int i)
{
	i = 0;

	if (i == 0)
	{
		printf("%d is zero", i);
	}
}
